---
tags:
  - 其他
---


设置vscode无高亮复制粘贴
## 匹配函数定义

匹配static开头的行
^static .*

匹配不以static开头的行
^(?!static\b).*$

匹配括号之间的所有内容
\((.*?)\)

匹配不以extern开头的行
^(?!extern\b).*$

匹配100之后
100.*

匹配行结尾的空格
[ ]+\n
## word文字生成表格
使用通配符整理好函数和变量定义后,使用word的插入-表格-通过文本生成就可以转化到表格了
